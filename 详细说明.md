# 详细说明

## 控制台按钮说明

**最大人数**

游戏本体仅支持 4 人。选择 8 / 12 / 16 / 24 / 32 后，建房时将按所选人数开设房间，
大厅中的人数滑块也会同步放宽。修改后需**重新建房**方可生效。

**关卡**

◀ ▶ 与下拉框仅用于「选择」，选定后需点击「前往选中关卡」，才会将整队迁移至该关卡。

「跳过当前关」：若本关存在出口，会先将全员送至出口区域，由游戏按正常流程结算过关；
未找到出口（例如仍处于大厅）时，则直接切换至下一关。

**队伍**

* 「全员集合到房主」：将队友移至房主身边，适用于走散或清点人数的情形
* 「全员进出口」：将所有人送至出口区域，人员到齐后由游戏自行结算
* 「掉队自动拉人」：默认关闭。开启后每隔数秒检查一次，与房主距离超过约 100 米且持续未跟上的玩家，
  会被自动拉回房主身边。前往出口途中开启最为省事。退出游戏后该开关自动复位为关闭。

**工具**

刷新状态、关卡列表（弹出窗口列出全部关卡，并标出当前关卡与选中关卡）、打开日志、启动游戏。

## 快捷键

| 按键 | 作用 |
| --- | --- |
| `F6` | 显示 / 收起控制台 |
| `F10` | 打开游戏内控制台（可输入下方指令） |
| `Ctrl+F9` | 全员集合到房主 |
| `Ctrl+Shift+F9` | 全员进出口 |
| `Ctrl+Shift+F10` | 跳过当前关 |
| `Ctrl+Shift+F4` | 掉队自动拉人 开 / 关 |
| `Ctrl+Shift+F6` | 上一个关卡（仅选择，不切换） |
| `Ctrl+Shift+F7` | 下一个关卡（仅选择，不切换） |
| `Ctrl+Shift+F8` | 切换至选中的关卡 |
| `Ctrl+Shift+F11` | 关卡列表 |
| `Ctrl+Shift+F12` | 状态 |

控制台按钮与快捷键功能相同。点击按钮后底部会显示执行结果；
若长时间无响应，通常说明游戏所在机器上的 mod 未在运行（重新启动游戏即可）。

`F6` 为全局热键，可能被截图 / 录屏 / 输入法占用。被占用时窗口标题会显示
「Ctrl+Alt+F6 显示 / 收起」，使用该组合键效果相同；若两者均被占用，重新双击
`HostPanel.bat` 同样可以唤出窗口。

## 控制台指令

按 `F10` 打开游戏内控制台，输入：

```
etb_hub                  查看状态
etb_players 16           修改人数（2-32，下次建房生效）
etb_levels               关卡列表
etb_level 8              切换至第 8 关（也可写关卡名）
etb_skip                 跳过当前关
etb_gather               全员集合到房主
etb_exit                 全员进出口
etb_assist on            掉队自动拉人 开
etb_assist off           掉队自动拉人 关
etb_help                 全部指令
```

## 常见问题

**修改人数后未生效**：需重新建房。已开设的房间人数不会变更。

**切关为什么不能连续点击**：切换地图会重建整个游戏世界，连续操作容易引发问题，
因此两次切换之间需间隔 10 秒，期间按钮显示「冷却中…」，稍候再点击即可。

**「全员进出口」无反应**：本关需先存在出口区域（即需处于有出口的地图中）。
控制台底部会明确提示是未找到出口，还是已找到但无法读取坐标，日志中亦有记录。

**是否开启「掉队自动拉人」**：开启只是更省事，手动点击「全员进出口」效果相同。

**12 人是否会卡顿**：人数较多时对房主网络要求较高。若第 11、12 名玩家出现明显卡顿，
可打开 `%LOCALAPPDATA%\EscapeTheBackrooms\Saved\Config\WindowsNoEditor\Game.ini`，
将 `MaxClientRate` 由 `250000` 调小（例如 `150000`）。

**游戏本体原本仅支持 4 人**：人数上限是额外扩展的，请勿用于公开房间，建议与熟人一同游玩。

**首次使用建议先做小规模测试**：人数与切关均依赖修改网络参数与蓝图函数实现。
建议先以 2 人开一局，将「切关」「全员进出口」各执行一次，确认无误后再进行十余人规模的游玩。

**高分辨率屏幕上窗口显示偏糊**：面板基于固定像素布局的旧框架，系统缩放 150% 时会被系统拉伸，
不影响使用。

**安装后游戏更新**：更新会覆盖 `Binaries\Win64`，注入随之失效，重新执行一次 `一键安装.bat` 即可
（存档与配置不受影响），完成后可用 `check.bat` 确认。

## 安装其他 pak mod

例如他人制作的图形化选关菜单。操作步骤如下：

1. 在解压出的文件夹中新建 `paks` 文件夹
2. 将所有 `.pak` 文件放入其中
3. 重新执行一次 `一键安装.bat`

安装时会自动将其放入 `…\Content\Paks\LogicMods`。
此类 mod 通常还需要作者提供的配套前置包（例如 `LevelSelector.pak` 需要其作者的
`Z_ModLoader_P`、`Z_DebugLog_P`），前置未放齐时游戏中不会出现菜单。

## 问题排查与卸载

**卸载**：移除游戏目录中的 mod、面板文件与桌面快捷方式，并删除 `Game.ini` 中的联机参数，
等同于恢复安装前的状态。命令为 `卸载.bat`（或 `uninstall.bat`）；加 `-RemoveUE4SS` 会连同注入所需的
运行时（`dwmapi.dll` 与 `ue4ss` 目录）一并清除 —— 游戏本体原本不包含这些文件。
默认将被删除的内容移入 `%LOCALAPPDATA%\EscapeTheBackrooms\ETB-uninstall-backup`
以备恢复，加 `-Purge` 则直接删除。

控制台底部会直接显示结果，例如「已将队员送进出口区域」「本关尚未找到出口区域」。
更详细的信息可查看以下两个文件：

* `…\Binaries\Win64\ue4ss\UE4SS.log`（mod 日志）
* 控制台同目录下的 `HostPanel.log`（控制台自身日志）

此外需注意：游戏内控制台（`F10`）与快捷键依赖注入组件在游戏启动时挂载，
因此**安装或修改设置后必须重新启动游戏**；重启后仍然无效时，重新执行一次 `一键安装.bat` 即可。

---

包内附带的 UE4SS 运行时为 MIT 许可的第三方组件（作者 Narknon 等），许可证原文位于
`runtime\ue4ss\LICENSE`，来源与版本说明见 `README.md` 的「第三方组件」一节。

作者：**彧晟Eason**
